
public class dvsff {

  
  /*
  // second push
  Message msg2 = new Message("Mathematics",
      "Time Complexity is a topic is Discrete Math.");
  msgStack.push(msg2);
  if(!msgStack.peek().getSUBJECT().equals("Mathematics"))
    return false;
  if(!msgStack.peek().getTEXT().equals("Time Complexity is a topic is Discrete Math."))
    return false;
  if (msgStack.isEmpty())
    return false;
  if (msgStack.size() != 2)
    return false;
  
  // third push
  Message msg3 = new Message("Comp Sci", "Java is not a dynamically typed language.");
  msgStack.push(msg3);
  if(!msgStack.peek().getSUBJECT().equals("Comp Sci"))
    return false;
  if(!msgStack.peek().getTEXT().equals("Java is not a dynamically typed language."))
    return false;
  if (msgStack.isEmpty())
    return false;
  if (msgStack.size() != 3)
    return false;
    */
  
  
  catch(Exception e) {
    System.out.print("Throws an unexpected Excpetion");
  }
  
  ////////////////////
  //try {}
  // catch (EmptyStackException ese) {

  // }
  //////////////////
}

/*
LinkedNode<Message> newMessage = new LinkedNode<Message>(element);

LinkedNode<Message> temp = this.top;


this.top = newMessage;
this.top.setNext(temp);
this.size++;
*/
